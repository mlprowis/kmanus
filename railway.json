{
  "build": {
    "installCommand": "npm_config_build_from_source=true npm install",
    "buildCommand": "npm run build --workspace=@grvt-grid/bot && cd packages/dashboard && npx vite build && mkdir -p packages/bot/dist/dashboard/public && cp -r packages/dashboard/dist/. packages/bot/dist/dashboard/public/"
  },
  "deploy": {
    "startCommand": "npm run start --workspace=@grvt-grid/bot",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
