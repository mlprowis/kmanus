{
  "deploy": {
    "startCommand": "sh -c 'echo STEP1 && node --version && echo STEP2 && ls /app/packages/bot/dist/dashboard/ && echo STEP3'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
